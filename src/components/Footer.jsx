export default function Footer() {
  return (
    <footer className="border-t-2 border-clay/20">
      <div className="max-w-4xl mx-auto w-full px-4 py-8">
        <p className="font-mono text-[11px] uppercase tracking-[0.15em] text-ink-soft/70">
          CareBuddy — works fully offline after the first page load, except the Google Fonts &amp; Tailwind fetch.
        </p>
      </div>
    </footer>
  );
}
